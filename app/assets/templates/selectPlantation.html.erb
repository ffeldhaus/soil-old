<div class="modal-header">
    <h1>Was soll angebaut werden?</h1>
</div>
<div class="modal-body">
    <ol class="ui-selectable plantation">
        <li ng-click="click(plantation)" class="ui-selectee" style="background-image: url(<%= Rails.configuration.assets.prefix %>/{{plantation | lowercase}}.jpg)" ng-repeat="plantation in plantations">{{plantation}}</li>
    </ol>
    {{msg}}
</div>